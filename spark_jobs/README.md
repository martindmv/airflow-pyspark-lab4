# spark_jobs/ (Track X only)

**Capstone Spark code belongs in** `include/team_<yourname>_spark.py` (called from your Airflow `@task`).

`daily_kpis.py` wraps the **minimal smoke** `lab4_starter` module for optional **Track X** (`spark-submit`). Not required to pass Lab 4.
